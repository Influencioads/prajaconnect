'use client';

import { ActivitiesView } from '@/components/crm/activities-view';

export default function ActivitiesPage() {
  return (
    <ActivitiesView
      title="Activities"
      description="Unified engagement center — calls, meetings, tasks, campaigns and field work across the constituency."
    />
  );
}
